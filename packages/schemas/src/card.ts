// The longest side a card may have, in characters: what a form accepts, an
// AI generation keeps and a word note's generated fields hold. One number,
// so web, mobile and the API cannot drift apart.
export const CARD_SIDE_MAX_LENGTH = 1000;
